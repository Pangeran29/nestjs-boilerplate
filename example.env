# APP
APP_NAME="Nest Boilerplate"
PORT="3000"
NODE_ENV="development" # "development" || "production"
PREFIX_NAME="api"

 
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="mypassword"
POSTGRES_DB="some_db_name"
DATABASE_URL="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}/${POSTGRES_DB}?schema=public"

# JWT
JWT_EXPIRATION="1h"
JWT_SECRET="%jkt*&h$#!)kjfbvh(d)fe"